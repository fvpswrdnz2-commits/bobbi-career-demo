## 2026-08-27：方案 B 升级为正式产品观感的高保真沟通版

- 用户反馈：上一版的桌面讲解侧栏、顶部方案横幅，以及“演示昵称”“演示简历”“模拟预约”等文案破坏代入感，视觉完成度低于已认可的早期移动端概念稿。
- 视觉重做：`demo/index.html` 改为只展示移动端产品；`demo/styles.css` 重建白底、墨水蓝、暖橙、浅薰衣草、细分隔线和简历批注感设计系统，并增加微信小程序状态栏、标题胶囊、统一线性 SVG 图标、底部导航和手机/桌面响应式容器。
- 流程补全：`demo/app.js` 现覆盖首页快捷方向与统一推荐、独立搜索与匹配理由、资料详情、未来 7 天档期、30/60/90 分钟套餐、本地文件选择、问题填写、服务规则确认、微信支付确认、支付成功、我的预约、个人中心和联系波比。
- 对外文案：所有用户页面删除“演示”“模拟”“方案 B”“演示昵称”“演示简历”“未做证明核验”等穿帮词；支付结果使用“会议链接稍后更新，请在咨询开始前查看”，订单使用昵称、标签和正式样式编号。
- 安全边界：页面表现接近正式流程，但不读取简历文件内容、不发起网络请求、不调用支付、不发通知或创建会议；只有最新一笔订单摘要写入浏览器本地存储。技术边界只保留在 `memory-bank/demo-design.md`、`solution-map.md` 和 `architecture.md`。
- 自动验证：`node --check` 覆盖 `demo/app.js` 与两个数据文件；`node demo/tests/check.mjs` 覆盖 10 条资料、术语防误标、6 个搜索首位结果、首页/搜索/详情/预约/支付/结果/预约列表以及用户界面穿帮词扫描，全部通过。
- 浏览器验证：已识别并接管用户打开的本地文件页，但内置浏览器安全策略拒绝对 `file://` 页执行跳转和刷新；依照安全要求未尝试绕过或切换其他浏览器，因此本轮没有把自动检查冒充为最新视觉截图验收。用户在当前预览标签手动重新加载即可看到新版，仍建议首次向 Bobby 展示前人工走一遍首页到支付结果。
- 方案关系：方案 A 的 V1.4 生产文档和第 1—124 步实施计划没有删除或降级；本次只升级方案 B 的沟通质量。

## 2026-08-27：方案 B 轻量沟通演示版完成

- 方案区分：新增 `memory-bank/solution-map.md` 和 `memory-bank/demo-design.md`；在生产版产品、技术栈和实施计划顶部标注“方案 A”。原第 1—124 步生产实施计划未改写，方案 A 第 5 步仍因外部条件未就绪而保持阻塞。
- 演示实现：新增独立 `demo/`，覆盖首页统一算法列表、编号/别名/一句话搜索、可解释匹配、资料详情、独立演示档期、模拟简历与问题、模拟预约、会议待更新状态和预约列表。
- 资料导入：结构化导入投稿 354、353、352、351、350、349、348、347、345、344 共 10 条，343 留作候补；演示昵称由投稿标题生成。
- 术语调研：新增 `demo/docs/tagging-research.md`，依据国家金融监督管理总局、证监会、PMI、CFA Institute 和 SAP 资料记录金租、公募机销、PMO、PIM、IB、前台岗等处理方式；不确定术语保持原文并列入 Bobby 复核清单。
- 隐私处理：未导入户籍信息；性别和政治面貌只保留原帖背景，不参与排序；全部资料标记为历史投稿导入、证明材料豁免且未做证明核验；预约演示只使用虚构简历名且不实际上传。
- 测试环境：Node.js 24.19.0、静态文件检查、受控内置浏览器。
- 自动测试：`node --check` 覆盖三个 JavaScript 文件；`node demo/tests/check.mjs` 覆盖 10 条资料完整性、投稿编号唯一性、术语防误标、户籍排除、6 个典型搜索、首页排序说明、资料页、模拟预约、结果页和预约列表，全部通过。
- 浏览器测试：尝试在 127.0.0.1 启动静态服务时被工作区权限拒绝；改用本地文件入口时又被内置浏览器 URL 安全策略拒绝。没有绕过或切换浏览器，因此真实视觉、控制台和逐按钮浏览器回归尚未完成。
- 结论：方案 B 的代码和自动回归通过，可作为 Bobby 沟通演示包；在普通浏览器中首次展示前，仍需补做桌面和手机两种视口的视觉验收。该结论不代表方案 A 已恢复或生产能力已完成。

## 2026-08-27：实施计划第 5 步进行中——外部账号与资质待人工确认

- 新增 `memory-bank/external-account-readiness.md`，覆盖微信小程序主体、开发权限、微信支付、订阅消息、腾讯云、COS、PostgreSQL、CloudBase、域名、SSL、CLS、SSM/CAM、短信和腾讯会议运营账号。
- 每个未准备项均已标记当前状态、负责人和最晚完成里程碑；当前全部为“未确认”，没有猜测为就绪。
- 测试环境：本地工作区与受控浏览器；本地只检查配置文件名和常见秘密模式，不读取或输出任何环境变量值。
- 实际结果：仓库未发现外部配置文件；常见秘密模式扫描通过，未发现匹配。受控浏览器安全策略禁止访问微信公众平台控制台，未发生登录或外部操作。
- 结论：第 5 步尚未通过。需要波比按准备清单第 3 节回复非秘密状态，随后才能验证主体关系、测试身份和开发权限；实施计划规定上一步未通过不得进入第 6 步。
- 自动续行复核：尝试只读检查腾讯云控制台登录状态时未获浏览器安全授权，没有绕过或切换其他浏览器；本机同时未发现腾讯云/CloudBase CLI、微信开发者工具 CLI 或腾讯会议桌面应用。外部账号状态仍缺少用户人工确认，结论不变。
- 波比后续确认：微信小程序未注册、微信支付商户未开通、微信订阅消息模板未申请、没有已备案域名；腾讯云账号已实名；腾讯会议只有个人版账号。
- 已将外部依赖从“未知”改为明确的“准备中/已就绪待技术验证”，并在准备清单补充合规咨询、主体注册、小程序认证、支付、域名备案、订阅模板、会议并发和腾讯云资源的推荐办理顺序。
- 当前结论：状态已明确但实际账号、主体关系和测试权限仍未就绪，第 5 步的控制台验证仍未通过。

## 2026-08-27：实施计划第 4 步完成——需求追踪表

- 新增 `memory-bank/requirements-traceability.md`，为页面、端到端流程、角色、前辈档案、搜索、档期、预约、支付、会议、改期、取消、结算、隐私、通知、经营数据、安全环境和核心数据实体建立唯一需求编号。
- 将产品文档第 21 节 39 条验收标准编号为 `ACC-001`—`ACC-039`，每条均映射到产品需求、实施步骤和具体测试场景。
- 建立反向追踪，显式覆盖实施计划的 133 个步骤标题（含 36.1—36.8、38.1 和 69.1），确保不存在无产品依据的实现步骤。
- 测试环境：macOS 工作区、zsh、ripgrep、awk、comm。
- 测试输入：产品第 21 节验收项目、全部需求定义行、全部实施步骤标题和追踪表六类目录。
- 实际结果：验收追踪 39/39、需求定义 ID 无重复、反向步骤 133/133、六类追踪目录 6/6，`TOTAL_FAILURES=0`。
- 结论：通过；后续实现和测试应引用相应需求 ID，产品变更必须同步维护追踪关系。

## 2026-08-27：实施计划第 3 步完成——架构记忆库核对

- 按实际仓库状态核对 `memory-bank/architecture.md`：当前只有文档基线和早期 `prototype/`，正式三端代码、数据库迁移与云资源均未创建，计划内容没有冒充已实现事实。
- 核对系统边界、模块职责、数据事实来源、外部服务、local/staging/production 环境隔离、安全不变量和当前待决策项。
- 补充模块化单体、不使用 Redis、不使用 Kubernetes、第一版不接腾讯会议 API 的具体取舍理由。
- 测试环境：macOS 工作区、zsh、ripgrep。
- 测试输入：微信原生小程序、React、NestJS、PostgreSQL、Prisma、pg-boss、COS、微信支付、CloudBase、CLS、SSM 共 11 个组件；PostgreSQL 事实来源、后端状态机、三环境隔离、人工会议边界、非微服务/Redis 边界共 5 项。
- 实际结果：组件 11/11、边界 5/5 全部命中，`TOTAL_FAILURES=0`。
- 结论：通过；架构文档能够解释数据存储、状态修改、外部隔离和第一版技术取舍。

## 2026-08-27：实施计划第 2 步完成——唯一产品设计基线

- 确认 `memory-bank/design-document.md` 是仓库中唯一标题为产品设计文档的 Markdown 文件；原型设计系统不是产品需求来源。
- 核对文档标题、状态“第一版需求已确认”、版本 V1.4 和更新日期 2026-08-27。
- 测试环境：macOS 工作区、zsh、ripgrep；扫描范围排除只读 `sources/` 和早期 `prototype/`。
- 测试输入：23 个一级章节计数，以及搜索、7 天档期、6 小时限制、15 分钟锁定、微信支付、2 小时改期、双方取消、7 天撤权、90 天删除和人工结算 11 项关键规则。
- 实际结果：唯一产品设计候选 1/1、元信息 4/4、一级章节 23/23、关键规则 11/11 全部通过，`TOTAL_FAILURES=0`。
- 结论：通过；V1.4 可作为后续需求追踪和验收的唯一产品基线。

## 2026-08-27：实施计划第 1 步完成——项目约束和输入文档

- 完整阅读：`AGENTS.md`、`memory-bank/architecture.md`、`memory-bank/design-document.md` V1.4、`memory-bank/tech-stack.md`、`memory-bank/implementation-plan.md` 和既有 `memory-bank/progress.md`。
- 新增 `memory-bank/project-baseline-checklist.md`，将第一版必须完成的范围、明确非目标、强制安全要求和文档优先级固化为可复核清单。
- 逐项对照产品文档第 3、18、20、21、23 节，覆盖微信支付、人工会议、人工结算、双角色、单一超级管理员、7 天档期、6 小时限制、15 分钟锁定、改期取消、隐私材料和“标签匹配＋三因素加权”搜索。
- 明确第一版不做渠道码、渠道归因、售后反馈、售后工单或 24 小时售后期，并记录群面和讲座仅保留三个低成本数据扩展点。
- 发现一处后续需消除的文档偏差：`tech-stack.md` 第 5.3 节曾写标签词典包含“版本”，但产品文档与架构基线明确第一版不建设词典版本系统；本步骤执行清单已按产品 V1.4 采用“不建立词典版本系统”，待实施计划第 36.1 步落实到数据模型。
- 测试环境：macOS 工作区、zsh、ripgrep；测试输入为基线清单中的 13 个强制关键词、产品文档 5 个指定章节标题和 5 条追踪表记录。
- 实际结果：13/13 强制项、5/5 来源章节、5/5 追踪记录全部命中，`TOTAL_FAILURES=0`。
- 结论：通过；可以进入实施计划第 2 步。

## 2026-08-27：产品基线更新至 V1.4

- 咨询到达约定结束时间后，双方订单直接显示“已完成”。
- 小程序订单状态不再包含“待结算”或“已结算”。
- 前辈端移除结算中心、待结算金额、预计结算金额和结算记录。
- 系统仍在波比后台独立计算80%/20%、生成待结算记录；波比在线下付款后只在后台登记。
- 已同步产品设计、实施计划、架构和技术栈；演示交互原型继续保持不变。

## 2026-08-27：产品基线更新至 V1.3

- 用户确认现有视觉稿的整体方向可用于后续开发对齐，不再继续维护交互原型。
- 首页改为轻松 Slogan 和单一算法列表，移除“波比精选”“最近上新”分区及简介中的可约时间文案。
- 小程序面向用户的称谓改为昵称、标签、“对方”“我的资料”等自然表达；内部学生/前辈角色模型不变。
- 会议未更新、已更新分别显示“稍后查看会议链接”“会议信息已更新”。
- 第一版移除售后反馈、售后工单和24小时售后期，约定结束后正常订单直接进入待结算。
- 新增受邀前辈资料增加可预约时间偏好，实际可售档期仍由波比后台确认维护。
- 已同步 `design-document.md`、`implementation-plan.md`、`architecture.md` 和 `tech-stack.md`；未修改演示交互原型。

## 2026-08-27：全页面交互原型

- 已完成学生端、前辈端和波比后台三套统一视觉概念。
- 已创建 `prototype/`，提供页面地图和 28 个业务页面的可点击展示。
- 演示原型曾覆盖预约主流程、改期/取消/售后分支、前辈资料与结算、后台前辈/档期/订单/会议/退款/结算/经营数据页面；其中售后页面已被 V1.3 废弃，仅作为未继续维护的早期演示内容保留。
- 已通过页面生成、导航目标、主要 HTML 结构和 JavaScript 语法检查。
- 尚需在可启动本地服务的环境中完成桌面与手机真实渲染验收；该限制不影响正式项目架构基线。
## 2026-08-27：方案 B 回归已认可原型并补齐双身份流程

- 视觉基线：以 `prototype/assets/concept-student.png`、`concept-senior.png` 和早期移动端页面的开放列表、橙色章节标记、浅紫匹配理由为母版，重构 `demo/styles.css`，没有继续扩大演示工程范围。
- 首页文案：Slogan 改为“求职有点绕，找个学长学姐聊聊”，删除解释搜索方式的提示段落；保持一个算法排序列表，不设置精选或上新分区。
- 资料文案：10 个公开昵称与标题移除“选手”，求职情况中的“最终结果/招聘结果”统一为“最终去向”。
- 预约流程：预约确认页移除协议勾选框；“上传简历”和“咨询问题”用星号标记必填；支付、支付结果和本地订单状态继续保留正式产品观感。
- 订单流程：新增订单详情、会议信息、改期和取消页面；订单进度使用“会议信息已更新”，改期文案使用“第二次发起改期”，取消规则不使用身份限定词。
- 咨询工作台：新增工作台、咨询列表、咨询详情、改期、取消、资料状态和完整资料填写页；工作台与咨询详情均提供明确入口，资料页包含可预约时间偏好、证明材料上传、保存草稿和提交审核；没有结算中心或金额信息。
- 自动验证：JavaScript 语法检查通过；`node demo/tests/check.mjs` 覆盖 10 条资料、6 个搜索查询、预约必填、支付结果、订单详情、改期取消、工作台、资料提交入口和用户界面禁用词，全部通过。
- 视觉验证限制：当前会话没有可调用的内置浏览器控制工具，无法对用户已打开的 `file://` 标签执行刷新和截图；未使用未获授权的浏览器替代方案，也没有把结构检查冒充为视觉验收。用户刷新当前标签即可查看新版。
- 生产基线同步：`design-document.md` 和 `implementation-plan.md` 已同步新 Slogan、首页提示语、预约必填星号与无独立协议复选框；方案 A 的生产范围、实施顺序和外部依赖状态保持不变。
## 2026-08-27：逐页对照早期原型并补齐沟通版

- 差异核对：新增 `memory-bank/demo-page-coverage.md`，将早期原型 28 个业务页面逐一映射到当前路由；明确售后反馈、咨询工作台结算中心 2 个页面已经废弃，不恢复；其余 26 个继续有效页面全部覆盖，并补充消息提醒、规则与隐私、咨询工作台取消预约 3 个后续确认页面。
- 预约方补全：`demo/app.js` 新增消息提醒、服务规则与隐私页面；“我的预约”支持待进行、已完成、已取消三种状态；取消后展示退款结果，订单完成按约定结束时间自动处理。
- 咨询工作台补全：咨询列表支持即将进行、已完成、已取消三种状态；咨询详情增加联系波比入口；继续保留资料状态、完整资料填写、可预约时间偏好、证明材料、草稿与提交审核，不恢复结算中心。
- 波比后台补全：新增 `demo/admin.html`、`demo/admin.css`、`demo/admin.js`，提供独立桌面后台，覆盖仪表盘、10份旧资料、审核与编辑、档期、订单、人工会议、退款与异常、80%/20%结算和经营数据共 10 个页面；后台不从普通小程序界面暴露入口。
- 交互补全：页面筛选标签可切换，档期可开关；邀请、旧资料导入和结算批次使用页面弹层；会议号与链接完整填写后可保存并显示“会议信息已更新”；关键按钮均有明确状态或反馈。
- 自动测试：`node --check demo/app.js`、`node --check demo/admin.js` 与 `node demo/tests/check.mjs` 全部通过；测试覆盖 10 份投稿资料、搜索与预约、双身份状态、消息与规则、10 个后台页面以及禁用文案。
- 视觉基线：重新查看 `prototype/assets/concept-student.png`、`concept-senior.png` 和 `concept-admin.png`，当前三端分别沿用白底移动端、咨询工作台与深蓝侧栏桌面后台方向。受控浏览器可识别用户打开的本地文件标签，但安全策略拒绝刷新 `file://` 页面；没有绕过该限制，最新渲染仍需用户手动刷新后查看。

## 2026-08-27：方案 B 正式回归用户指定的 `prototype/` 原型

- 用户明确指出当前正确视觉基线是 `prototype/index.html#/senior-consults`，不是后来单独重画的 `demo/` 外壳。方案 B 唯一展示入口已改为 `prototype/index.html`；`demo/` 界面文件停止演进，只保留十条共享资料、标签词典和历史对照。
- 保留旧原型的手机框、页面目录、页面说明、留白、开放列表、橙色章节标记和浅紫匹配理由。点击“专注预览”后会隐藏顶部原型栏、目录、检查说明和页面标题，只展示产品画面，避免向 Bobby 暴露“演示”语境。
- 首页使用“求职有点绕，找个学长学姐聊聊”，移除精选/上新分区与简介中的可约文案；十位历史投稿资料全部进入同一稳定排序列表。
- 新增 `prototype/src/search.js`，支持投稿编号精确直达、术语别名匹配和可解释排序；验证 `#347`、`公募机销`、`PMO` 均首位命中对应资料。
- 预约咨询侧已同步必填星号、无协议复选框、会议链接稍后更新、昵称与标签订单、会议信息已更新、第二次发起改期、取消规则无身份前缀，以及咨询结束后直接完成。
- 提供咨询侧已移除“学生/前辈”用户文案、结算中心和金额；补齐咨询列表、咨询详情、改期、取消、联系波比、资料状态、可预约时间和证明材料入口。
- 波比后台资料列表改为十份历史投稿；退款页只保留退款与异常，后台继续保留人工会议录入和80%/20%线下结算登记。
- 测试环境使用工作区内置 Node.js 24.19.0。`prototype/src/app.js`、`mobile-pages.js`、`admin-pages.js`、`search.js` 语法检查通过；`prototype/tests/render-check.mjs` 通过，结果为 27 个业务页面、28 个导航入口、10 条资料、全部跳转目标和主要 HTML 结构有效。
- 内置浏览器安全策略仍禁止自动刷新或截图 `file://` 页面，因此未把结构测试表述为视觉回归通过。用户需在当前标签手动刷新；首次向 Bobby 展示前仍应人工走一遍首页、搜索、预约、订单和咨询工作台。

## 2026-08-28：Bobby 说明页完成，公开发布包已准备

- 将 `prototype/index.html#/map` 从页面结构图改为简短的 Bobby 产品说明，包含产品价值、三条建议体验路线、主要降本点、需要确认的三个问题和当前体验边界；其余业务页面保持不变。
- 新增 `memory-bank/bobby-guide.md` 作为说明文字版，并新增 `prototype/tests/guide-check.mjs` 检查说明页文案、路线和响应式样式。
- 新增独立 `share-site/` 发布壳：根路径自动进入 Bobby 说明页，完整复制当前 `prototype/` 与十位结构化资料，不修改方案 A 生产架构，也不替换已认可原型。
- `share-site/public/` 已补充纯静态 `index.html`，整目录可以直接交给静态托管服务，打开根链接同样进入 Bobby 说明页。
- 已生成根目录 `Bobby-Product-Demo.zip`，解压后即为上述纯静态分享包。
- 已创建 Sites 项目并把项目标识写入 `share-site/.openai/hosting.json`，后续必须复用该项目，不得重复创建。
- 自动测试：`share-site/tests/public-entry.test.mjs` 2/2 通过；原型渲染检查通过（27 个业务页面、28 个导航入口）；说明页检查通过。
- 发布阻塞：当前受控环境无法解析飞书认证域名及 Sites 源码仓库域名；源码仓库推送失败，因此尚未保存版本、部署或生成任何可访问 URL。公开链接仍未完成，不能把本地 `file://` 地址发给 Bobby。

## 2026-08-28：中国大陆公开 Demo 已发布

- 网络条件恢复后，复用既有 Sites 项目完成源码推送、构建、版本保存和公开部署；备用全球链接为 `https://bobbi-career-demo.josiema18800126467.chatgpt.site`。
- 为满足中国大陆访问要求，使用妙搭 HTML 托管重新发布同一份 `share-site/public/` 静态内容，应用 ID 为 `app_17cyv0x8ae6`。
- 国内首选分享链接为 `https://onhzes97tk.feishuapp.com/app/app_17cyv0x8ae6`；访问范围已验证为 `All`，`require_login=false`，任何人无需登录即可打开。
- 外部访问检查：根链接返回页面并自动进入说明页；原型入口和十位资料文件均可直接获取。响应经过火山引擎 DCDN，并显示上海电信节点信息。
- 新增 `memory-bank/deployment-and-domain.md`，明确 Demo 阶段不需要自购域名；正式合作后再确定域名归属、完成实名认证，并依据服务器是否位于中国大陆决定 ICP 备案。

## 2026-08-28：补充 Vercel 与 Cloudflare 手动部署指南

- 新增 `memory-bank/vercel-cloudflare-deployment-guide.md`，以现成的 `Bobby-Product-Demo.zip` 为输入，分别记录 Vercel Drop 和 Cloudflare Pages Direct Upload 的无 Git、无构建操作步骤。
- 明确两种平台只作为全球备用和作品集链接：Vercel 官方不保证中国大陆可达性，Cloudflare 官方说明 Pages 不在中国网络中；国内体验继续使用妙搭公开链接。
- 补充两种平台的自定义域名、DNS 和 HTTPS 步骤，以及 Cloudflare Direct Upload 无法原地切换为 Git 集成的限制。

## 2026-08-28：Vercel 生产链接已代用户完成部署

- 复用本机此前 Dancecard 项目使用的 Vercel 登录身份 `fvpswrdnz2-commits`，无需用户重新授权。
- 创建 Vercel 项目 `bobbi-career-demo`，将 `share-site/public/` 作为纯静态站点部署至 Production。
- 稳定别名为 `https://bobbi-career-demo.vercel.app`，无需登录即可访问。
- 外部检查通过：根页面 HTTP 200 并自动进入 Bobby 说明页；原型入口和十位资料文件均返回成功，投稿 354 与 344 数据存在。
- Vercel 仅作为全球备用和作品集链接；中国大陆体验仍以妙搭链接为首选。

## 2026-08-28：独立三入口页已发布

- 新建 `bobbi-portal/`，与现有 `prototype/`、`share-site/` 和完整 Demo 部署完全隔离。
- 页面只展示三个圆形入口：“预约咨询”“提供咨询”“波比后台”；页面地图、产品说明和流程讲解均未放入该页面。
- 三个入口分别直达完整原型的首页、咨询工作台和波比后台仪表盘，原有完整 Demo 内容及 URL 未修改。
- 自动测试 `node bobbi-portal/tests/check.mjs` 通过，覆盖入口数量、名称、目标路由、圆形样式、响应式规则和禁用内容。
- 真实浏览器完成 1280×720 桌面端与 390×844 手机端核对：三个入口均为正圆、文字与图标清晰、手机端无横向溢出。
- 新建 Vercel 项目 `bobbi-demo-portal` 并发布至 Production；稳定链接为 `https://bobbi-demo-portal.vercel.app`，外部 HTTP 200 检查和线上 HTML 路由检查均通过。
- 用户点名的 Zara HTML skill 未安装且当前插件目录无可安装候选，因此按技能回退规则使用现有前端构建能力完成，没有扩大功能范围。

## 2026-08-28：删除页面地图并改用 Frontend Slides 独立说明

- 用户澄清此前语音中的 skill 指 `Frontend Slides`，并明确原型里的“页面地图 / 给 Bobby 的说明”整页不要保留。
- 从 `prototype/src/data.js`、`app.js`、`styles.css` 和测试中删除页面地图分组、路由、说明内容与全部专用样式；无效路由、品牌按钮和公开根入口均改为 `#/student-home`。
- 原型顶部现在恰好只有“预约咨询”“提供咨询”“波比后台”三个菜单，左侧对应 10、7、10 个业务页面，共 27 页。
- 用 `frontend-slides` skill 将 `bobbi-portal/index.html` 重做为 5 页单文件产品提案，采用 DanceCARD 式黑橙编辑视觉、1920×1080 固定舞台、键盘/滚轮/触控翻页和本地文字编辑。
- Frontend Slides 第 3 页提供三个真实原型链接；产品提案与产品原型继续使用两个独立 Vercel 项目。
- 自动测试通过：原型 27/27 页面与三个菜单、Frontend Slides 5 页与三个链接、公开入口 2/2 测试均通过。
- 真实浏览器逐页检查 5 张桌面截图；在 390×844 手机视口验证固定舞台居中且页面无横向溢出；线上控制台无错误。
- 已覆盖发布 `https://bobbi-career-demo.vercel.app` 和 `https://bobbi-demo-portal.vercel.app`。线上检查确认完整原型不含“页面地图”，产品提案为 5 页且三个链接正确。

## 2026-08-28：按 DanceCARD 历史反馈重做产品提案

- 从历史会话中逐条恢复用户对 DanceCARD HTML 的反馈，形成自查清单：降低模板化 AI 文案、不逐句附英文、每页职责清楚、实机截图足够大、产品演示页明确标注、角色流程不混画、关键链接突出、首尾页均可进入产品。
- 将 `bobbi-portal/index.html` 从 5 页重做为 7 页，视觉统一为完整原型的海军蓝、亮橙、浅紫和白色体系，不再沿用与产品割裂的黑橙编辑风格。
- 新增 5 张真实原型界面截图，分别展示预约首页、智能搜索、确认预约、咨询工作台和后台仪表盘；预约侧与提供咨询/后台分别占一页，三方业务闭环另用泳道页表达。
- 更新 `bobbi-portal/tests/check.mjs`，覆盖 7 页固定舞台、三入口、5 张截图、两张“实际产品演示”页、原型配色、键盘/触控、禁用页面地图与模板化对照文案。
- 本地自动检查通过：产品提案检查通过，内联脚本语法通过，完整原型仍为 27 个业务页面与 27 个导航入口。
- 覆盖发布至 `https://bobbi-demo-portal.vercel.app`。真实浏览器逐页核对 1280×900 桌面视口并完成翻页验证；390×844 手机视口下固定舞台为 390×219、页面宽度为 390×390，无横向溢出。

## 2026-08-28：完整露出产品原型的手机圆角底端

- 用户通过截图澄清问题位于产品原型的手机模型，不是 Frontend Slides 的翻页控制条；已撤回并重新发布此前对 `bobbi-portal/` 的误改。
- 第二张截图进一步确认用户指的是带页面目录和右侧说明的普通原型工作台，不是专注预览；先通过缩短手机验证了底部裁切原因。
- 用户随后要求删除中间舞台重复的页面名称与“主按钮可以点击”提示。已从 `prototype/src/app.js` 删除整个 `stage-heading`，释放约 64px 高度，并将手机高度改为 `min(790px, calc(100vh - 152px))`、最低 500px；手机明显拉长且底部仍有空间。
- 专注预览继续采用 `28px 36px 64px` 内边距，860px 以下采用 `18px 8px 56px`；两种模式都不改变手机内部页面或底部导航高度。
- 同步更新 `share-site/public/prototype/` 中的脚本和样式，确保本地原型和 Vercel 发布快照一致；渲染检查验证重复标题栏不存在、普通工作台手机高度以及两档专注预览留白。
- 自动检查通过：原型仍为 27 个业务页面与 27 个导航入口，Frontend Slides 恢复原布局，公开发布包 2/2 通过，原型样式快照与发布快照完全一致。
- 已覆盖发布 `https://bobbi-career-demo.vercel.app` 和回滚后的 `https://bobbi-demo-portal.vercel.app`。线上普通工作台在 1280×720 视口下标题栏不存在，手机高度 568px、底边位于 676px，距离视口底部 44px；专注预览在 768×512 下保留 56px，圆角和阴影均完整可见。

## 2026-08-31：准备 GitHub 与 Vercel 自动发布

- 对照 DanceCARD 确认其采用根 Git 仓库、`main` 分支跟踪 GitHub 的交付方式；Bobby 项目此前则是 Vercel CLI 手工发布，尚未连接 GitHub。
- 将 `share-site/` 原有的局部 Git 元数据安全移至 `/tmp/bobbi-share-site-git.ILptBt/share-site.git`，并在项目根目录初始化统一的 `main` 仓库；旧记录没有删除。
- 新增根 `.gitignore`，排除 `sources/`、依赖、构建产物、环境变量、本地 Vercel 状态、数据库与压缩包；暂存文件检查未发现这些目录或常见密钥格式。
- 自动检查全部通过：原型 27/27 页面、Frontend Slides 7 页与 5 张真实截图、历史 Demo 10 条资料与角色流程、公开发布包 2/2。
- 计划创建私有 GitHub 仓库 `fvpswrdnz2-commits/bobbi-career-demo`，并把现有 `bobbi-career-demo`、`bobbi-demo-portal` 两个 Vercel 项目分别连接到 `share-site/public` 和 `bobbi-portal`。外部上传与连接需在敏感资料确认后执行。
- 用户确认敏感资料上传后，已创建私有 GitHub 仓库 `https://github.com/fvpswrdnz2-commits/bobbi-career-demo`，推送根提交并让本地 `main` 跟踪 `origin/main`。
- 安装 Vercel GitHub App 时采用“Only select repositories”，只授予 `bobbi-career-demo` 仓库权限；两个既有 Vercel 项目均已连接该仓库，生产分支均为 `main`。
- Vercel Root Directory 已分别设置为：完整原型 `share-site/public`、Frontend Slides `bobbi-portal`。后续交付改为测试通过后提交并推送，由 Vercel 自动部署，不再手工执行生产部署命令。
- 新增中文 `README.md` 与英文 `README.en.md`，包含双语切换、在线体验、功能范围、目录结构、本地检查、双方案边界和数据使用说明。
- 用户知悉仓库包含 Bobby 联系方式、十份公开投稿资料和完整产品文档，并明确要求保留现有内容原样公开；GitHub 仓库可见性据此调整为 Public。
